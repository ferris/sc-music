// Take your patching solution from the previous assignment
// Schedule a new patching solution that takes effect after a certain amount of time has passed


Task {
	Ndef(\input) <>> Ndef(\process1) <>> Ndef(\process2) <>> Ndef(\output);

	numBeats.wait; // number of beats is up to you

	Ndef(\input) <>> Ndef(\process3) <>> Ndef(\process 4) <>> Ndef(\ouptuts)
}

// DUE MONDAY
